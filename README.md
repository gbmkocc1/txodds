# Flask IP App – Technical Assignment

This project contains:

A simple Python Flask app that returns the user’s public IP address

Dockerfile for building the app image

GitHub Actions workflow to build & push Docker images

Kubernetes manifests

Example Terraform configuration for provisioning EKS

## Docker Hub Image
Image: gbmkocc1/flask-ip-app:latest
